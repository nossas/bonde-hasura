table:
  name: widget_donation_stats
  schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - widget_id
        - stats
      filter: {}
  - role: user
    permission:
      columns:
        - widget_id
        - stats
      filter: {}
