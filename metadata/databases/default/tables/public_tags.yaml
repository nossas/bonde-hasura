table:
  name: tags
  schema: public
select_permissions:
  - role: common_user
    permission:
      columns:
        - id
        - name
        - taggings_count
        - label
        - kind
      filter: {}
